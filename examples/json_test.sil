#examples/json.sil

var employees = (("examples/employees.json".read()).json()).mget("employees");

print "Done parsing json\n";
printHash(employees, 3, 0);

print "\n";

for var i = 0; i < employees.length(); i += 1; {
    print (employees[i]).mget("name") + ": " + (employees[i]).mget("email") + "\n";
    //printHash(employees[i], 3, 0); print "\n";
}
